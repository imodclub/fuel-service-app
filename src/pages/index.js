import Layout from '../components/Layout';
import { Typography } from '@mui/material';

export default function Home() {
  return (
    <Layout>
      <Typography variant="h4">ยินดีต้อนรับสู่โปรแกรมเชื้อเพลิงและค่าใช้จ่าย</Typography>
    </Layout>
  );
}